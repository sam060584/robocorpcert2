channels:
  # Define conda channels here.
  - conda-forge

dependencies:
  # Define conda packages here.
  # If available, always prefer the conda version of a package.
  # Installation will be faster and more efficient.
  # https://anaconda.org/search
  - python=3.9.13
  - pip=22.1.2

  - pip:
      # Define pip packages here.
      # https://pypi.org/
    - rpaframework==28.2.0        # https://rpaframework.org/releasenotes.html
    - robocorp==1.6.2             # https://pypi.org/project/robocorp
    - robocorp-browser==2.2.2     # https://pypi.org/project/robocorp-browser
     
